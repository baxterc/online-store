import { productScore } from 'online-store/helpers/product-score';
import { module, test } from 'qunit';

module('Unit | Helper | product score');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productScore([42]);
  assert.ok(result);
});
